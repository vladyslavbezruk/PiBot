from telethon import TelegramClient, types
#import asyncio

api_id = 16415945
api_hash = 'ba1cf49ce4ad6033a0977a76578dc1d0'

def get_info():
    with TelegramClient('anon', api_id, api_hash) as client:
        client.loop.run_until_complete(await client.send_message('me', 'Hello, myself!'))

        zuser = client.get_entity('vladyslavbezruk')

	try:
	    if isinstance(user.status, types.UserStatusOnline):
	        print("Online")
	    elif isinstance(user.status, types.UserStatusOffline):
	        print(str(user.status.was_online).replace("+00:00", ""))
	except:
	    print("Unable to get last seen")

get_info()
